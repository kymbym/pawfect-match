{
  "name": "pawfect-match",
  "version": "1.0.0",
  "description": "find your furrever friend today",
  "main": "index.js",
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^19.4.0",
    "@commitlint/config-conventional": "^19.2.2",
    "husky": "^9.1.5",
    "lint-staged": "^15.2.9",
    "prettier": "^3.3.3"
  },
  "lint-staged": {
    "*.js": "prettier --write"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "dependencies": {
    "express": "^4.19.2"
  }
}

